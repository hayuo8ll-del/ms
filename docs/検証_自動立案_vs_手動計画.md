# 検証: 自動立案の計画は、手で立てている計画になっているか

対象: `claude/production-planning-automation-6l0150` (`58b0fd0`)
検証日: 2026-07-29
使用した実データ: THM生産台帳2026 / THM短期投入予定表 / TA1_投入計画(7-8月) / FeliCa実計画

## 検証結果と、その後の修正

**2026-07-29 に検証 → 同日、指摘した項目を修正しました。** 以下は修正前の検証結果です。
修正内容と修正後の実測は末尾の「E. 修正と効果」にまとめてあります。

要点だけ先に:

| 指標(実データ 2026-07-29〜08-31) | 修正前 | 修正後 |
|---|---|---|
| 納期遅れ製番 | 16 | **8** |
| 遊休能力 | 484,920台 | **124,160台** |
| 工程別の台数欠損(ANT/TAL/MIL) | 5% / 10% / 5% | **0% / 0% / 0%** |
| 警告の行数 | 134行 | **21行** |
| FeliCa完成日MAE(7/1–8/31) | 9.42稼働日 | **8.73稼働日** |
| MAEの期間依存レンジ | 3.87〜18.18 | **4.14〜9.12** |
| Lite-S 完成日バイアス | +15.0稼働日 | **+2.71稼働日** |

CAP表(設備Cap/機種対応表)の取り込みと号機単位の割り付けは「F. CAP表の取り込み」に、
稼働号機11台の確定とそれで判明した表の不整合は「F-3」に書いてあります。

---

## 結論(先に)

**まだ「同じ計画」にはなっていない。** 取り込み(パース)は全ファイル正常だが、
**割り付けロジックが現場の流し方を再現していない。**

最も分かりやすい証拠 — 7/29〜7/31 の HAL 投入:

| 日 | あなたのTA1計画 | 自動立案 |
|---|---|---|
| 7/29 | **90,000** = Lite-S 40k + MOT2 20k + SuicaⅢ 20k + さそり交通 10k | **60,000** = MOT2 のみ |
| 7/30 | **90,000** = Lite-S 20k + SuicaⅢ 40k + さそり交通 10k + さそり金融 20k | **57,000** = MOT2 のみ |
| 7/31 | **90,000** = 同様に4機種 | **53,000** = MOT2 のみ |

あなたは毎日4機種を並行させてライン能力を使い切っている。自動立案は1機種しか流さず、
ライン能力の半分近くを空けている。**A勤限定切替の実装方法**が原因(下記 A-1)。

7月の実績(TA1赤字)で見ると、あなたのラインは **13稼働日すべてで複数機種を並行**させ、
**平均3.38機種/日**。自動立案は平均2.29機種/日で、しかも立ち上がりの3日は1機種。

FeliCa実計画との製番単位の照合では **完成日MAE 8〜9.4稼働日**(CLAUDE.md記載の2.66ではない)。

---

## 0. 取り込みは全て正常

これは問題なし。4ファイルとも既存パーサーで正しく読めた。

| ファイル | 結果 |
|---|---|
| THM生産台帳 | 101製番 / **機種解決できない行 0件** / 総需要 4,584,702台 / 完成目標 6/29〜9/29 |
| THM短期投入予定表 | 赤字MIL実績 **117製番・5,673,254台**(台帳外の製番も含むため、実際に需要から控除されたのは **2,181,120台**)。TAL実績 1,980,370台 |
| TA1_投入計画 | 赤字HAL実績 **13日・1,108,235台**(独立に手で数えた値と完全一致) |
| FeliCa実計画 | 310製番行(台数があるのは55行、carryover除外後に残るのは39行)、灰色非稼働日 **7/20・8/11・8/14** を検出 |

`parse_ta1_hal_actuals` / `parse_thm_shortterm_actuals` の赤字判定も、独立パースと
突き合わせて一致を確認した。**実績反映の配線は正しく動いている。**

---

## A. 計画そのものが手計画とずれる要因

### A-1. 【最重要】A勤限定切替が、同日並行生産を潰している

`allocate_bottleneck` の docstring はこう書いている:

> キャパの小さい機種(例: Lite-S)が残したライン能力は、同日に別機種が別号機グループで
> 並行して使う(現場のTA1_生産計画と同じ形)

ところが A勤限定切替は、その並行投入を「切替」とみなして塞ぐ。判定に使っている
`used_fraction = 1 - line_remaining / cap_today` は **ライン全体の消化率**であって、
時刻の代理になっていない。別号機グループが朝から空いていても、先行機種がライン能力を
食っていれば「もうB勤だから切替不可」と判定される。

7/29時点の残需要(2,403,582台 / 60製番)を 7/29〜8/31 に立案した実測:

| | A勤限定切替 ON(現行) | OFF |
|---|---|---|
| **納期遅れ製番** | **16** | **10** |
| 遊休能力 | 484,920台 | 368,680台 |
| 平均並行機種数 | 2.29 | 2.78 |
| 立ち上がり3日 | MOT2 単独 60k/57k/53k | MOT2+SuicaⅢ+さそり交通+SD3 等 3〜5機種 |
| A勤繰下げ警告 | 40件 | 0件 |

**この制約を外すだけで納期遅れが16製番→10製番に減り、遊休能力が116,240台減る。**
そして外した後の形のほうが、あなたの実績(毎日3〜5機種並行)に近い。

あなたの7月実績の実際の姿:

```
2026-07-01 さそり金融20k + さそり交通20k + SuicaⅢ40k          = 80k
2026-07-03 さそり金融10k + さそり交通20k + SuicaⅢ30k + MOT220k = 80k
2026-07-08 さそり交通10k + SuicaⅢ10k + Lite-S30k + 部分リライト10k + MOT220k = 80k
```

なお**切替の頻度そのものは合っている**(あなたの実績 0.69回/稼働日 vs 自動立案
0.58回/稼働日)。問題は回数ではなく、「並行して立ち上げてよい」という現場の運用を
モデルが表現できていないこと。

> 直し方: 切替可否の判定を「ライン消化率」ではなく **その機種が使う号機グループの
> 消化率**(または明示的な勤務帯)で行う。少なくとも、先行機種の機種別キャパ上限で
> 余った能力を別機種が埋めるケースは切替扱いから外す。

### A-2. 祝日が稼働日として数えられている(実データで確認)

FeliCa の灰色セルには **7/20(海の日)・8/11(山の日)・8/14(お盆)** が入っている。
一方 `config/bottleneck_planning.json` の `nonWorkingDays` は **空**。

結果、あなたのTA1が 0台 と置いている日に、自動立案はフル生産を割り付ける:

```
2026-08-11  あなた 0 台  /  自動立案 120,480 台
2026-08-14  あなた 0 台  /  自動立案 115,280 台
```

祝日を反映すると工程別の欠損も一気に悪化する(下表)ので、**照合や立案の前に必ず
「非稼働日カレンダー取込(FeliCa)」を実行する**運用にするか、祝日を config の初期値
として持たせる必要がある。

### A-3. シフトモードの答えが、入力した end_date で変わる

`choose_shift_mode` は「期間需要 ÷ 稼働日数」で必要日次レートを出すため、**各ロットの
納期を見ずに**、画面で指定した終了日だけでモードが決まる。同じ台帳・同じ開始日で:

| end_date | 稼働日 | 必要レート | 選択モード | 納期遅れ |
|---|---|---|---|---|
| 2026-08-15 | 13 | 184,891 | 22h (120k/日) | 16 |
| 2026-08-31 | 24 | 100,149 | **22h (120k/日)** | 16 |
| 2026-09-15 | 35 | 68,674 | **16h (90k/日)** | 13 |
| 2026-09-30 | 46 | 52,252 | 16h (90k/日) | 13 |

9月納期のロットまで8月中に作ろうとするので 22H(夜勤増強)になる。**あなたの実績は
80〜90k/日 = 16H相当**で、22Hでは回していない。終了日の入れ方ひとつで「夜勤を増やすか」
という原価に直結する答えが変わってしまうのは危うい。必要レートは**納期までの残稼働日で
ロット別に**見るべき。

### A-4. 上流/下流工程の台数が、計画期間の端で静かに消える

`expand_to_stages` は期間外に出たセルを破棄し、工程あたり1行の汎用警告を出すだけ。
実データ(残需要2,403,582台)での実測:

| 工程 | 祝日なし(現行config) | 祝日反映後 |
|---|---|---|
| ANT | 97.5% (欠損 60,320) | 92.9% (欠損 169,920) |
| TAL | **95.1%** (欠損 117,320) | **90.6%** (欠損 226,920) |
| HAL | 100.0% | 95.4% |
| MIL | 95.4% (欠損 109,920) | **87.4%** (欠損 303,120) |

画面と Excel の「生産計画(機種×日)」の TAL 行は需要の 90〜95% しか表示していない。
この表で材料を手配すると足りなくなる。月初の上流投入は前月からの繰り越し(前段WIP)
として明示するか、計画期間を実際より前後に広げて立案し表示だけ絞るのが筋。

### A-5. A勤繰下げ警告が事実と違い、大量に出る

警告文は「翌稼働日の朝に開始します」と言うが、翌日も同じ理由で再び繰り下がるので実際には
開始しない。実データで **40件**。同一機種の同一文言が並び、その中に本当に見るべき警告
(納期遅れ16製番など)が埋もれる。機種ごとに集約し「〜まで開始できません」に改めるべき。

---

## B. 「合っているか」を測る照合機構のバイアス

### B-1. 【最重要】報告される精度が、計画期間の取り方で3.87〜18.18稼働日まで振れる

同じ台帳・同じ FeliCa で、期間だけを変えた実測:

| 計画期間 | future_only | 照合製番 | 完成日MAE | 完成日bias | 投入日MAE |
|---|---|---|---|---|---|
| 7/01–7/31 | true | 15 | **3.87** | −3.33 | 5.33 |
| 7/01–7/31 | false | 10 | 6.40 | −3.80 | 6.14 |
| 7/01–8/15 | true | 25 | 6.84 | +0.36 | 7.44 |
| 7/01–8/15 | false | 21 | 6.48 | +0.19 | 7.13 |
| 7/01–8/31 | true | 33 | 7.97 | +3.06 | 8.00 |
| **7/01–8/31** | **false** | **31** | **9.42** | **+5.16** | **9.19** |
| 7/01–9/30 | false | 33 | **18.18** | +15.27 | 16.81 |

**バイアスの符号すら反転する**(1か月なら「3〜4日早い」、2か月なら「5日遅い」)。
CLAUDE.md にある「完成日MAE 2.66稼働日」は、狭い窓・少ない母数(10〜15製番)での値で、
FeliCa がカバーする7〜8月全体で測ると **8〜9.4稼働日**になる。
**現状、この数字は「精度」として引用できる状態にない。**

原因は A-3 と同じで、期間を伸ばすほどシフトモードが下がって全体が後ろへ広がるため。
精度を報告するなら、**FeliCa の実データ範囲に期間を固定する**運用を決めるべき。

### B-2. 較正の推奨値がグリッドの端に貼りつき、実行ごとに変わる

`calibrate` の探索範囲と、実データでの推奨値:

| パラメータ | 探索範囲 | configの現行値 | 今回の推奨 | 位置 |
|---|---|---|---|---|
| ANT | (−3, −2, −1) | −1 | **−3** | **下端** |
| TAL | (−2, −1) | −2 | −2 | 下端 |
| MIL | (0, 1, 2) | +2 | **0** | **下端** |
| A勤割合 | (0.4, 0.5, 0.6) | 0.4 | **0.6** | **上端** |

現行 config の値(前回較正の採用値)は ANT −1 / MIL +2 / A勤 0.4 で、今回の推奨は
**すべて逆の端**。しかも4つとも範囲の端に貼りついている。これは最適値がグリッド外に
あることを示していて、較正が収束していない。改善幅も 9.42 → 8.64 稼働日と小さく、
**オフセット調整では埋まらない誤差**であることを裏づけている。

### B-3. 誤差の大半は Lite-S(Mies) 1機種に集中している

機種別の予実タイミング差(our − FeliCa、正 = 自動立案が遅い):

| 機種 | 完成日bias | 完成日MAE | 投入日bias | n |
|---|---|---|---|---|
| **Lite-S(Mies)** | **+15.0** | **16.5** | +14.0 | 12 |
| さそり金融 | +2.75 | 2.75 | +0.75 | 4 |
| SuicaⅢ | −2.36 | 4.00 | −3.73 | 11 |
| さそり交通 | −1.25 | 9.75 | −1.75 | 4 |

**Lite-S だけが3週間遅れる。** 他機種は±2〜3日程度。Lite-S は機種別キャパが
30,720/日(16H)と最も小さいため、EDD順で後回しにされると挽回できない。これは
リード時間オフセットの問題ではなく **HALのキャンペーン順序の問題**で、CLAUDE.md の
診断は正しい。逆に言えば、**Lite-S の順位付けを直せば全体MAEの大半が落ちる。**

実際あなたのTA1 8月計画は Lite-S を毎日20〜40k で通し続けている(8月に黒字で入って
いるのはほぼ Lite-S だけ)。自動立案は Lite-S を後半に寄せている。

### B-4. 照合できているのは FeliCa 全体の一部

- FeliCa の製番行 310 のうち、7〜8月に台数があるのは **55行**(255行は全ゼロ)
- 台帳の101製番と FeliCa の製番が一致するのは **75件**
- carryover除外(同日Line-In+Completion)で 19件中16件が消え、残り **39行**
- 結果、照合できた製番 = **31〜33件**(FeliCa全体の約1割)

**なお、carryover除外は実データ上は妥当だった。** 消えているのは例えば

```
製番 6WMA5ENN (RC-S103/JW16)  Line-In 7/16: 57,600  /  Completion 7/16: 57,600
```

のように**投入と完成が同日・同数**の行で、これは確かに先週までの計画台数の転記であって
実生産ではない。前回レポートでこの除外を「削りすぎ」と書いたが、実データで確認した結果
**この判定は正しい**。訂正する。

母数が31件に絞られること自体は避けられないが、**画面には「FeliCa 310製番中31製番で照合」
と母数を出すべき**(現状は照合できた数だけが出る)。

### B-5. 細かい点

- **計画期間外の日付が端の稼働日に丸められる。** `_nearest_index` は期間外も丸めるため、
  期間外のロットが「照合できた」ことになり誤差が期間幅で頭打ちになる。期間外は `None` を
  返して母数から外すべき。7/01–9/30 の窓で MAE が18.18まで伸びたのは実際のずれだが、
  逆に狭い窓では頭打ちで小さく見える。
- **`_with_offsets` が `lead_offset_by_product` を落とす。** 較正が評価する「現行値」が
  機種別オフセット設定時に別物の計画になる。現 config は未使用なので今は影響しないが、
  Phase 4 を使った瞬間に壊れる。1行で直る。
- **役割ラベルの表記ゆれで照合が黙って0件になる。** 製番(col4)は `.strip()` しているが
  `role == "Line-In"` は完全一致比較。末尾空白ひとつで読み取り0件になる(再現確認済み)。
- **`matched` は完成日のペア数のみ。** 投入日MAEの母数は画面に出ない。
- **TA1 に出てくる機種コード `A22F` / `S113` が `productAliases` に無い。** 今回は台数が
  0なので影響しないが、稼働したら計画から落ちる。
- **サマリーの「TA1 HAL実績反映(日数)」が 0 と出る。** 実績(7/1〜7/17)が計画開始日
  7/29 より前なので窓外になるだけで、動作は正しい。ただ 0 と出ると読み取り失敗に見える。

なお、**自分の計画をそのまま FeliCa 形式にして読ませる往復テストでは全MAEが0**になった。
照合の骨格そのものは健全で、上記はいずれも運用条件と端の処理の問題。

---

## C. 何を直せば「手計画と同じ」に近づくか

優先順位順。上2つで大半が動く。

| # | 項目 | 実データでの効果 | 難易度 |
|---|---|---|---|
| 1 | A勤限定切替の判定を号機グループ単位(または勤務帯)に変える (A-1) | 納期遅れ 16→10製番、遊休能力 −116,240台、並行機種数が実績の姿に近づく | 中 |
| 2 | Lite-S のような低キャパ機種の順位付けを見直す (B-3) | 完成日bias +15.0 稼働日の解消 = 全体MAEの大半 | 中 |
| 3 | 祝日を config 既定値にする / 照合前のカレンダー取込を必須化 (A-2) | 8/11・8/14 のフル生産割り付けが消える | 小 |
| 4 | 必要日次レートを納期ベースで算出する (A-3) | end_date で 22H/16H が変わる不安定さの解消 | 中 |
| 5 | 上流/下流の期間外セルをWIPとして明示 or 期間を広げて立案 (A-4) | TAL 5〜9%、MIL 5〜13% の欠損解消 | 中 |
| 6 | 照合期間を FeliCa の実範囲に固定して精度を報告する (B-1) | 数字が引用できるようになる | 小 |
| 7 | `_nearest_index` の期間外を `None` に (B-5) / `_with_offsets` の欠落修正 | 較正が実力を測るようになる | 小 |
| 8 | A勤繰下げ警告の集約と文言修正 (A-5) | 本当に見るべき警告が読めるようになる | 小 |

1 と 2 を直したうえで、期間を 7/1–8/31 に固定して `/api/bottleneck/validate` を回し直すのが
次に取るべき数字。現状のベースラインは **完成日MAE 9.42 / bias +5.16 / 照合31製番**。

---

## D. 再現方法

```bash
cd backend
python3 -m pytest                     # 100 passed
python3 ../docs/verify_comparison.py  # 照合機構の端の挙動(合成データ)
python3 ../docs/verify_real_data.py \
    --ledger 台帳.xlsx --felica FeliCa.xlsx \
    --thm THM短期.xlsx --ta1 TA1.xlsx    # 本レポートの実データ計測
```

---

## E. 修正と効果 (2026-07-29 実施)

上の A / B で挙げた項目を実装し、同じ実データで測り直した結果。

### E-1. 何を直したか

| # | 指摘 | 修正内容 | 場所 |
|---|---|---|---|
| 1 | A-1 A勤限定切替が並行生産を潰す | 切替可否を「ライン消化率」から **号機グループの空き**に変更。その日すでに流している機種の機種別キャパ合計がライン能力に届かなければ、別グループが終日空いている＝朝から段取りできるので制約対象外 | `allocate_bottleneck` |
| 2 | B-3 Lite-S が3週間遅れる | 投入順を素のEDDから **最小スラック順**へ。スラック = 納期までの稼働日数 − 同機種で自分より納期の早い分も含めた所要日数(機種別キャパで割る)。キャパ未定義なら従来どおりEDD | `_sequence_lots` |
| 3 | A-3 モードが end_date で変わる | 必要日次レートを **納期ベースの累積フィージビリティ**へ。各納期時点の累積需要 ÷ その納期までの稼働日数 の最大値。最大能力でも間に合わない納期はモード選択から除外(遅れ警告の仕事) | `required_daily_rate` |
| 4 | A-4 工程の台数が期間端で消える | 工程展開用に **前後へ伸ばした表示軸** `stage_days` を導入。上流の前倒し・下流の後ろ倒しを軸の上に置けるようにした。画面・Excelともこの軸を列に使い、計画期間外の列は淡く表示 | `extend_working_days` / `expand_to_stages` / `main.py` / `app.js` / `bottleneck_export.py` |
| 5 | A-2 祝日が稼働日扱い | `nonWorkingDays` に FeliCa 由来の 7/20・8/11・8/14 を初期値として投入 | `config/bottleneck_planning.json` |
| 6 | A-5 警告が読めない | A勤繰下げを**機種ごとに1行**へ集約。実績由来の「除外した製番」「台帳にない製番」も**件数+サンプル1行**へ集約 | `allocate_bottleneck` / `apply_actuals` |
| 7 | B-2 較正がグリッド端に貼りつく | 探索範囲を拡大し、**工程順の単調性 ANT ≤ TAL ≤ HAL(0) ≤ MIL を強制**。従来値 ANT −1 / TAL −2 は「TALがANTより先に投入される」物理的にありえない組み合わせだった | `calibrate` |
| 8 | B-5 期間外を端に丸める | `_nearest_index` は計画期間外を `None` にして母数から外す | `felica_calibration` |
| 9 | B-5 機種別オフセットが落ちる | `_with_offsets` が `lead_offset_by_product` を引き継ぐ | `felica_calibration` |
| 10 | B-4 母数が見えない | 照合レポートに `felica_lots` / `felica_active_lots` / `start_matched` を追加し、画面に「310製番(うち当該期間に台数があるのは39製番)に対し完成日33製番・投入日32製番で照合」と表示 | `compare_plans` / `main.py` / `app.js` |

修正7を反映して config のオフセットを **ANT −4 / TAL −4 / HAL 0 / MIL 0、A勤割合 0.5** に更新した
(完成日MAE 8.85 → 7.97)。

### E-2. 効果(実データ 2026-07-29〜08-31、残需要 2,403,582台 / 60製番)

| 指標 | 修正前 | 修正後 |
|---|---|---|
| 納期遅れ製番 | 16 | **9** |
| 遊休能力 | 484,920台 | **249,080台** |
| 平均並行機種数 | 2.29 | **2.55**(あなたの実績 3.38) |
| A勤繰下げ警告 | 40件 | **5件** |
| 警告の総行数 | 134行 | **21行** |
| A勤限定切替 ON と OFF の納期遅れ差 | 6製番 | **0製番** |

立ち上がり3日の HAL:

```
修正前  7/29 MOT2 60k              7/30 MOT2 57k              7/31 MOT2 53k
修正後  7/29 MOT2 60k+Suica 60k    7/30 MOT2 60k+Suica 20k    7/31 MOT2 60k+Lite-S 30k
             = 120,000                  +SD3 7k+Lite-S 40k         +さそり交通 26k
                                        = 126,662                  = 116,080
あなた  7/29 Lite-S40k+MOT220k+Suica20k+さそり交通10k = 90,000（4機種）
```

工程別の台数欠損:

| 工程 | 修正前(祝日反映後) | 修正後 |
|---|---|---|
| ANT | 94.8%(欠損 124,160) | **100.0%** |
| TAL | 89.8%(欠損 244,160) | **100.0%** |
| MIL | 95.4%(欠損 109,742) | **100.0%** |

FeliCa照合(実績控除前の需要で立案した計画に対して):

| 計画期間 | 修正前 完成MAE | 修正後 完成MAE | 修正前 bias | 修正後 bias |
|---|---|---|---|---|
| 7/01–7/31 | 6.40 | **3.86** | −3.80 | +0.43 |
| 7/01–8/15 | 6.48 | 8.82 | +0.19 | −2.18 |
| 7/01–8/31 | 9.42 | **7.97** | +5.16 | +2.33 |
| 7/01–9/30 | **18.18** | **6.52** | +15.27 | +1.91 |

期間の取り方による振れが **3.87〜18.18 → 3.86〜8.82** に収まった(モードが期間で
ひっくり返らなくなったため)。

機種別タイミング差:

| 機種 | 修正前 完成bias | 修正後 完成bias | 修正前 投入bias | 修正後 投入bias | n |
|---|---|---|---|---|---|
| Lite-S(Mies) | **+15.0** | **+4.14** | +14.0 | **+0.62** | 14 |
| SuicaⅢ | −2.36 | −1.0 | −3.73 | −2.45 | 11 |
| さそり交通 | −1.25 | −3.75 | −1.75 | −6.0 | 4 |
| さそり金融 | +2.75 | **+11.25** | +0.75 | +6.25 | 4 |

### E-3. 残っている差(正直なところ)

- **さそり金融が悪化した(+2.75 → +11.25、n=4)。** スラック順にすると、機種別キャパが
  大きく余裕のあるさそり金融は後ろへ回る。あなたは逆に7月上旬から流している。サンプルが
  4製番と少ないうえ、材料入荷や客先優先など台帳に載っていない理由で前倒ししている可能性が
  高い。台帳外の優先度をモデルが知らない以上、これは今のデータでは埋められない。
- **並行機種数はまだ届いていない**(自動 2.55 / あなたの実績 3.38)。A勤限定切替を完全に
  外すと 3.05 まで上がるが、切替が本当にA勤限定なら外すのは不正確。号機グループを明示的に
  持たせない限りこれ以上は詰められない。
- **シフトモードは依然 22H を選ぶ**(あなたの実績は 80〜90k/日 = 16H相当)。残需要
  2,403,582台を24稼働日で流すには 100k/日 必要なので、計算としては正しい。差は「9月納期の
  ロットを8月中に作るか、9月へ送るか」という方針の違いで、計画期間の切り方の問題。
- **照合の母数は33製番**(FeliCa 310行中、当該期間に台数があるのは39行)。母数が小さいので
  MAEは1〜2稼働日の変動に敏感。精度を継続的に見るなら、期間を FeliCa の範囲に固定した
  上で追うこと。

### E-4. テスト

`cd backend && python3 -m pytest` → **113 passed**(修正前は100)。追加した13件は、
上の各修正が意図どおり動くこと(および機種別キャパが無いときは従来のEDDへ縮退すること)を
実データで見えたケースに沿って検証している。


---

## F. CAP表(設備Cap/機種対応表)の取り込み — 2026-07-29 追加

「号機グループのマスタがあればもっと正確にモデル化できる」と書いた件。CAP表を頂いたので
取り込んで、号機単位の割り付けに置き換えた。

### F-1. 取り込んだもの

`backend/cap_import.py` を追加し、`config/bottleneck_planning.json` へ書き出す。

| 取り込み内容 | 結果 |
|---|---|
| 号機マスタ(`machinesByMode`) | 4モード × 13号機(ANT 2 / TAL 2 / HAL 5 / MIL 4)。号機ごとの日次能力 + 機種別の○/△ |
| 機種別キャパ(`productDailyCapsByMode`) | **旧configのハードコード値と完全一致**(転記は正しかった) |
| 機種別名(`productAliases`) | 16件 → **67件**(フルのモデル名55件 + 枝番を落としたルートコード12件) |
| レシピコード(`recipeCodes`) | `A02F`→さそり金融 等9件。TA1_投入計画の機種コード列がこれ |
| 号機台数(`machineCounts`) | ANT 1→2、TAL 2、HAL 5、MIL 4 に修正 |

**表の内部整合も検証できた。** 号機の能力から機種別キャパを再計算すると、8機種中6機種が
表の値と完全一致した(例: Lite-S 42,240 = MIL#3 の1台ぶん、MOT2 60,000 = HAL 2台ぶん)。

### F-2. 号機単位の割り付けに置き換えた効果

| 指標(7/29〜8/31) | 機種別キャパのみ | 号機マスタあり |
|---|---|---|
| 平均並行機種数 | 2.55 | **2.82**(あなたの実績 3.38) |
| A勤繰下げ警告 | 5件 | **3件** |
| 納期遅れ製番 | 9 | 9 |
| FeliCa完成日MAE(7/1–8/31) | 7.97 | 8.70 |

並行度は上がったが、**日付精度(MAE)はむしろ少し悪化した**。原因は次項。

### F-3. 稼働号機を11台に確定 → CAP表の 機種別キャパ 列が実態と合っていない

**稼働号機は TAL#2/#3、HAL#5〜#9、MIL#3/#5/#6/#7 の11台**(ANT工程は号機を持たない)と
確認いただいたので、マスタをその11台に絞った。CAP表には稼働していない列や、シフトモードで
ラベルが食い違う列(22Hだけ `ANT#1`、16H以下は `TAL#1`。しかも22Hの能力86,400が16Hの
108,000より小さい)が残っているため、`cap_import.ACTIVE_MACHINES` で明示的に絞り込む。

これで **HAL#1/HAL#2 は存在しない**ことが確定した(シート上部の注記は古い)。つまり
さそり金融/SuicaⅢ の 機種別キャパ 120,000/日 は**出せない数字**である:

| 機種 | 機種別キャパ(22H) | 使えるHAL号機 | 実際に出せる |
|---|---|---|---|
| さそり金融 | 120,000 | HAL#5/#6/#7(3台) | **90,000** |
| SuicaⅢ | 120,000 | HAL#5/#6/#7(3台) | **90,000** |
| さそり交通 | 120,000 | HAL#5/#6/#8/#9(4台) | 120,000 ✓ |

**CAP表の「機種別キャパ列」と「機種×号機の○/×」のどちらかが実態と合っていません。**
HAL#8/#9 でさそり金融・SuicaⅢ が本当に作れないなら列を90,000へ、作れるなら○へ。

計画側は**号機を正**として扱い、出せる90,000/日で組んだうえで警告を出すようにした:

```
さそり金融: CAP表の機種別キャパ 120,000/日(22h)は、稼働号機でHALに使える能力
90,000/日 を超えています(差 30,000)。出せる 90,000/日 で計画しました。
機種別キャパ列か、機種×号機の可否(○/×)のどちらかが実態と合っていないので
確認してください。
```

この「出せる値まで抑える」処理が効果的だった。抑えないと、実際には出せない日産を前提に
スラック(所要日数)を見積もるため、その機種の着手が遅れる:

| 指標(7/29〜8/31) | 号機マスタ無し | 全号機(抑制なし) | **稼働11台+抑制** |
|---|---|---|---|
| 納期遅れ製番 | 9 | 9 | **8** |
| 遊休能力 | 249,080台 | 248,320台 | **124,160台** |
| 平均並行機種数 | 2.55 | 2.82 | **2.86** |
| A勤繰下げ警告 | 5件 | 3件 | 4件 |
| FeliCa完成日MAE(7/1–8/31) | 7.97 | 8.70 | 8.73 |

**遊休能力が半減した**(249,080 → 124,160台)。納期遅れも1件減。ただし FeliCa との
日付一致(MAE)は号機モデル無しのほうが僅かに良い — 号機モデルは物理的に正しい計画を
出すが、日付精度は 33製番の母数では差が有意とは言えない範囲(8.0 vs 8.7)で、
差の主因は さそり金融(n=4)である(下記 F-5)。

### F-4. その他、表について気づいた点

- **16H/11H/8H ブロックの5列目のラベルが `TAL#1`、22H ブロックだけ `ANT#1`。**
  能力も 22H 86,400 < 16H 108,000 と逆転していて、22Hのほうが少ない。どちらかが誤記だと
  思われる。号機台数は名前が素直な22Hブロックから数えている。HAL(ボトルネック)には影響なし。
- **16H の MOT2 の機種別キャパ 20,000** は、号機(HAL#8/#6 の2台×20,000=40,000)から見ると
  半分。22Hは 60,000 = 2台×30,000 で整合しているので、16Hだけ手入力が古い可能性がある。
- `lineDailyCapacities`(16H 90,000 / 22H 120,000)は、CAP表の号機合計(HAL 16H 100,000 /
  22H 150,000)より低い実務レート。あなたの実績が80〜90k/日なので妥当だが、
  **号機ではなくこのライン上限が効いている日がほとんど**である点は認識しておくとよい。

### F-5. 残っている最大の差: さそり金融

機種別タイミング差(our − FeliCa、正=自動立案が遅い):

| 機種 | 完成日bias | 完成日MAE | 投入日bias | n |
|---|---|---|---|---|
| **さそり金融** | **+12.0** | 12.0 | +9.75 | 4 |
| さそり交通 | −4.5 | 5.5 | −7.5 | 4 |
| Lite-S(Mies) | +2.71 | 10.71 | +1.5 | 14 |
| SuicaⅢ | −0.18 | 6.18 | −1.73 | 11 |

Lite-S は +15.0 → **+2.71** まで直った(投入日は +14.0 → +1.5)。SuicaⅢ もほぼ0。
残るはさそり金融だけで、**あなたは7月上旬から流しているのに自動立案は8月下旬に回す**。

さそり金融は日産90,000/日で需要746,840台=8.3稼働日ぶんしかなく、納期も9/29まであるため
スラック計算上は「後回しでよい」機種になる。前倒しする理由(材料入荷・客先優先・
金融カードの検査リードなど)が台帳に載っていないため、モデルからは判断できない。

**もし前倒しの理由が決まったルールなら教えてください**(例「金融は常に月初」「客先Aは
最優先」)。ロットの優先度として入れられます。

### F-6. 較正の再確認

工程オフセットの探索範囲を ±8 まで広げ、号機マスタ・非稼働日も較正側に渡して
立案と同条件にした。結果:

| | 現行config | 推奨 |
|---|---|---|
| ANT / TAL / HAL / MIL | −4 / −4 / 0 / 0 | −5 / −5 / 0 / 0 |
| A勤割合 | 0.5 | 0.7 |
| 完成日MAE | 8.73 | 8.70 |

改善は 0.03稼働日で、**オフセットはほぼ収束している**(端に貼りついてもいない)。
残差はリードタイムではなくキャンペーン順序なので、config は −4/−4 のまま据え置いた。
A勤割合の推奨0.7は「A勤制約をほぼ効かせない」方向だが、現場の実ルールなので追従しない。
